# URL of apps/api, used server-side by Server Components for direct fetches.
# Phase 0 only — Phase 1 introduces a Route Handler BFF layer per ADR-003 once
# there's a session cookie to protect, and pages stop calling the API directly.
API_URL=http://localhost:3000
